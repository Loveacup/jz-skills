[pytest]
asyncio_mode = auto
# 默认只跑确定性单测；集成（含联网）测试 opt-in：`pytest -m integration`
addopts = -m "not integration"
markers =
    integration: marks tests as integration tests (run with '-m integration')
